return {
  "marko-cerovac/material.nvim",
  lazy = false,
  priority = 1000,
  -- The variant (oceanic) is NOT a setup option -- it is picked by loading the
  -- `material-oceanic` colorscheme (see config.lua), which sets
  -- vim.g.material_style. opts here is for everything else material exposes.
  opts = {},
}
